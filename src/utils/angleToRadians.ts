/**
 * Converts an angle from degrees to radians.
 * @param degreeAngle - Angle in degrees.
 * @returns The equivalent angle in radians.
 */
export function angleToRadians(degreeAngle: number) {
    return (Math.PI / 180) * degreeAngle;
}
